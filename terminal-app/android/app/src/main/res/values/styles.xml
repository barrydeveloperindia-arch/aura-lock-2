<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme — dark kiosk background -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">#0a0f1e</item>
        <item name="colorPrimaryDark">#0a0f1e</item>
        <item name="colorAccent">#3b82f6</item>
        <item name="android:windowBackground">#0a0f1e</item>
        <item name="android:statusBarColor">#0a0f1e</item>
        <item name="android:navigationBarColor">#0a0f1e</item>
    </style>

    <!-- Fullscreen kiosk theme (no action bar, dark background) -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">#0a0f1e</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Launch splash theme -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

</resources>